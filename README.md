# geopulse Experience Builder app

## Setup

Install pnpm if not already installed

```powershell
cd server
npm i -g pnpm
```

Initialize pnpm

```powershell
pnpm ci
```

Do the same for the client

```powershell
cd ../client
pnpm ci
```
